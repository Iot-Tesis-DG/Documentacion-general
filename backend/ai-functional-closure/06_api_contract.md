# 06 — API

Lecturas exponen `nivel_riesgo`, `confianza_ia`, `modelo_version` y alias
`model_version`, origen, estado, motivo y `estado_sensores`. Campos IA son
nullable. OpenAPI confirmado. Fechas serializan ISO 8601. JWT/RBAC preservado.
