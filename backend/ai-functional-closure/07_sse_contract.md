# 07 — SSE

SSE tipado: `lectura`, `alerta`, `episodio_actualizado`, `recuperacion`,
`fallo_sensor`, `inferencia_omitida`. MQTT duplicado se descarta antes de
publicar segundo evento lógico. Cliente React consume lectura, fallo sensor e
inferencia omitida.
