# 02 — Suite backend

Resultado final: `134 passed` con Python 3.12.13. Cubre JWT/RBAC, ingesta,
deduplicación, sensor nulo, 0 C, IA, checksum, trazabilidad, hash concurrente,
SSE y seguridad. Warnings: `starlette.testclient`, `crypt`, `joblib` y claves
JWT cortas sólo en fixtures.
